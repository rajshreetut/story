git init (adds git)

git add chapter1 (adds file to staging area)

git add . (adds all files to staging area)

git status (shows added files into staging area)

git commit -m "basic commads" (commits version)

git log (shows all the commits)

git diff (shows the difference in versions)

git checkout chapter2 (goes to the previous version)